extend type Query {
  article(id: Int!): Article
}

extend type Article implements ContentType{
  id: Int!
  uuid: String!
  entity: String!
  bundle: String!
  title: String!
  url: String!
  created: String!
  changed: String!
  language: String!
  seoTitle: String!
  content: [ContentElement]
}
