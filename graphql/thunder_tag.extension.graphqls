extend type Query {
  tag(id: Int!): Tag
}

extend type Tag implements ContentType {
  id: Int!
  uuid: String!
  title: String!
  entity: String!
  bundle: String!
  url: String!
  created: String!
  changed: String!
  language: String!
}
