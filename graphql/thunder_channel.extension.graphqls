extend type Query {
  channel(id: Int!): Tag
}

extend type Channel {
  id: Int!
  uuid: String!
  title: String!
  entity: String!
  type: String!
  url: String!
  created: String!
  changed: String!
  language: String!
}
