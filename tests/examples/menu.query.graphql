query ($path: String!) {
  page(path: $path) {
    menus(ids: ["main"]) {
      name
      id
      items {
        title
        url
        inActiveTrail
        children {
          title
          url
          inActiveTrail
        }
      }
    }
  }
}
